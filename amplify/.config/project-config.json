{
  "projectName": "greenbrick",
  "version": "3.1",
  "frontend": "javascript",
  "javascript": {
    "framework": "react",
    "config": {
      "SourceDir": "src",
      "DistributionDir": "out",
      "BuildCommand": "yarn install --frozen-lockfile && yarn build",
      "StartCommand": "yarn start"
    }
  },
  "providers": [
    "awscloudformation"
  ]
}